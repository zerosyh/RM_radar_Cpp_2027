# ============================================================
# converter_config.yaml — 激光雷达-相机融合参数（方案二 lidar_scheme 专用）
# ============================================================
# 用于 Converter.py 和 radar.py，配置外参、内参、聚类和点云滤波参数。
# 修改场景：更换相机/镜头、重新标定、调整聚类效果。
# ============================================================

params:
  max_depth: 20          # 最大深度（m），超出的点云被过滤
  width: 3072            # 相机原始分辨率宽度（像素），用于深度图投影
  height: 2048           # 相机原始分辨率高度（像素），用于深度图投影

calib:
  extrinsic:
    # 旧标定 (2026-01-24):
    #   R.data: [0.01914, -0.99981, 0.00462, 0.00132, -0.00459, -0.99999, 0.99982, 0.01915, 0.00123]
    #   T.data: [-0.05301, 0.04890, -0.07717]
    R:
      rows: 3
      cols: 3
      dt: d
      data: [-0.00285, -1.00000, 0.00113, -0.00663, -0.00111, -0.99998, 0.99997, -0.00285, -0.00662]
    T:
      rows: 3
      cols: 1
      dt: d
      data: [0.00765, 0.06018, 0.00000]
  intrinsic:
    cx: 1505.65003
    cy: 1068.52082
    fx: 3446.07130
    fy: 3435.46526
  distortion:
    rows: 1
    cols: 5
    dt: d
    data: [-0.107215, 0.201526, -0.000783, 0.000129, 0.000000]

cluster:
  eps: 0.40 # eps是DBSCAN算法的一个参数，它是一个浮点数，用来控制邻域的大小
  min_points: 10 # min_samples是DBSCAN算法的一个参数，它是一个整数，用来控制核心点的最小个数
  print_progress: False # 是否打印进度

filter:
  nb_neighbors: 20
  std_ratio: 2.0
  voxel_size: 0.035

field:
  Red:
    left_up: [0., 5233, 800]
    left_down: [0., 5233, 0]
    right_down: [4063, 4464, 0]
    right_up: [4063, 4464, 305]
  Blue:
    left_up: [0., 5233, 800]
    left_down: [0., 5233, 0]
    right_down: [4063, 4464, 0]
    right_up: [4063, 4464, 305]
  test:
    left_up: [0,0,125] # 22.5 12.5
    left_down: [0,0,0]
    right_down: [225,0,0]
    right_up: [225,0,125]
