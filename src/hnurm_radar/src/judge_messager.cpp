#include <rclcpp/rclcpp.hpp>
#include <detect_result/msg/locations.hpp>
#include <yaml-cpp/yaml.h>
#include <thread>
#include <mutex>
#include <vector>
#include <cstdint>
#include <cstring>
#include <fcntl.h>
#include <unistd.h>
#include <termios.h>

using namespace std::chrono_literals;

// ── CRC 表 ──────────────────────────────────────────────
static const uint8_t CRC8_TABLE[256] = {
    ,
};

static const uint16_t CRC16_TABLE[256] = {
    0x0000,0x1189,0x2312,0x329b,0x4624,0x57ad,0x6536,0x74bf,0x8c48,0x9dc1,0xaf5a,0xbed3,0xca6c,0xdbe5,0xe97e,0xf8f7,
    0x1081,0x0108,0x3393,0x221a,0x56a5,0x472c,0x75b7,0x643e,0x9cc9,0x8d40,0xbfdb,0xae52,0xdaed,0xcb64,0xf9ff,0xe876,
    0x2102,0x308b,0x0210,0x1399,0x6726,0x76af,0x4434,0x55bd,0xad4a,0xbcc3,0x8e58,0x9fd1,0xeb6e,0xfae7,0xc87c,0xd9f5,
    0x3183,0x200a,0x1291,0x0318,0x77a7,0x662e,0x54b5,0x453c,0xbdcb,0xac42,0x9ed9,0x8f50,0xfbef,0xea66,0xd8fd,0xc974,
    0x4204,0x538d,0x6116,0x709f,0x0420,0x15a9,0x2732,0x36bb,0xce4c,0xdfc5,0xed5e,0xfcd7,0x8868,0x99e1,0xab7a,0xbaf3,
    0x5285,0x430c,0x7197,0x601e,0x14a1,0x0528,0x37b3,0x263a,0xdecd,0xcf44,0xfddf,0xec56,0x98e9,0x8960,0xbbfb,0xaa72,
    0x6306,0x728f,0x4014,0x519d,0x2522,0x34ab,0x0630,0x17b9,0xef4e,0xfec7,0xcc5c,0xddd5,0xa96a,0xb8e3,0x8a78,0x9bf1,
    0x7387,0x620e,0x5095,0x411c,0x35a3,0x242a,0x16b1,0x0738,0xffcf,0xee46,0xdcdd,0xcd54,0xb9eb,0xa862,0x9af9,0x8b70,
    0x8408,0x9581,0xa71a,0xb693,0xc22c,0xd3a5,0xe13e,0xf0b7,0x0840,0x19c9,0x2b52,0x3adb,0x4e64,0x5fed,0x6d76,0x7cff,
    0x9489,0x8500,0xb79b,0xa612,0xd2ad,0xc324,0xf1bf,0xe036,0x18c1,0x0948,0x3bd3,0x2a5a,0x5ee5,0x4f6c,0x7df7,0x6c7e,
    0xa50a,0xb483,0x8618,0x9791,0xe32e,0xf2a7,0xc03c,0xd1b5,0x2942,0x38cb,0x0a50,0x1bd9,0x6f66,0x7eef,0x4c74,0x5dfd,
    0xb58b,0xa402,0x9699,0x8710,0xf3af,0xe226,0xd0bd,0xc134,0x39c3,0x284a,0x1ad1,0x0b58,0x7fe7,0x6e6e,0x5cf5,0x4d7c,
    0xc60c,0xd785,0xe51e,0xf497,0x8028,0x91a1,0xa33a,0xb2b3,0x4a44,0x5bcd,0x6956,0x78df,0x0c60,0x1de9,0x2f72,0x3efb,
    0xd68d,0xc704,0xf59f,0xe416,0x90a9,0x8120,0xb3bb,0xa232,0x5ac5,0x4b4c,0x79d7,0x685e,0x1ce1,0x0d68,0x3ff3,0x2e7a,
    0xe70e,0xf687,0xc41c,0xd595,0xa12a,0xb0a3,0x8238,0x93b1,0x6b46,0x7acf,0x4854,0x59dd,0x2d62,0x3ceb,0x0e70,0x1ff9,
    0xf78f,0xe606,0xd49d,0xc514,0xb1ab,0xa022,0x92b9,0x8330,0x7bc7,0x6a4e,0x58d5,0x495c,0x3de3,0x2c6a,0x1ef1,0x0f78,
};

static uint8_t crc8(const uint8_t* d, size_t n) {
    uint8_t c = 0xff;
    for (size_t i = 0; i < n; ++i) c = CRC8_TABLE[c ^ d[i]];
    return c;
}
static uint16_t crc16(const uint8_t* d, size_t n) {
    uint16_t c = 0xffff;
    for (size_t i = 0; i < n; ++i) c = (c >> 8) ^ CRC16_TABLE[(c ^ d[i]) & 0xff];
    return c;
}

// ── 帧工具 ──────────────────────────────────────────────
static std::vector<uint8_t> buildFrame(uint16_t cmd_id, const std::vector<uint8_t>& data) {
    std::vector<uint8_t> buf;
    buf.push_back(0xA5);                            // SOF
    uint16_t dlen = data.size();
    buf.push_back(dlen & 0xff);                     // data_length LO
    buf.push_back((dlen >> 8) & 0xff);              // data_length HI
    static uint8_t seq = 0;
    buf.push_back(seq++);                           // seq
    uint8_t c8 = crc8(buf.data(), 4);
    buf.push_back(c8);                              // header CRC8
    // cmd_id
    buf.push_back(cmd_id & 0xff);
    buf.push_back((cmd_id >> 8) & 0xff);
    // data
    buf.insert(buf.end(), data.begin(), data.end());
    // tail CRC16 (over header+cmd+data, 5+2+n bytes)
    uint16_t c16 = crc16(buf.data(), buf.size());
    buf.push_back(c16 & 0xff);
    buf.push_back((c16 >> 8) & 0xff);
    return buf;
}

// ── 裁判系统通信节点 ────────────────────────────────────
class JudgeMessager : public rclcpp::Node {
public:
    JudgeMessager() : Node("judge_messager") {
        auto cfg = YAML::LoadFile("configs/main_config.yaml");
        my_color_ = cfg["global"]["my_color"].as<std::string>("Red");
        my_id_ = (my_color_ == "Red") ? 9 : 109;
        field_w_ = cfg["scenes"]["competition"]["field_width"].as<float>(28.0f);
        field_h_ = cfg["scenes"]["competition"]["field_height"].as<float>(15.0f);

        auto comm = cfg["communication"];
        port_ = comm["port"].as<std::string>("/dev/ttyUSB0");
        int bps = comm["bps"].as<int>(115200);

        openSerial(port_, bps);

        rclcpp::QoS qos(rclcpp::KeepLast(5));
        qos.best_effort();
        sub_ = create_subscription<detect_result::msg::Locations>(
            "location", qos, std::bind(&JudgeMessager::cb, this, std::placeholders::_1));

        thread_ = std::thread(&JudgeMessager::loop, this);
        RCLCPP_INFO(get_logger(), "JudgeMessager ready. port=%s color=%s id=%d",
                    port_.c_str(), my_color_.c_str(), my_id_);
    }

    ~JudgeMessager() {
        running_ = false;
        if (thread_.joinable()) thread_.join();
        if (fd_ >= 0) close(fd_);
    }

private:
    std::string my_color_, port_;
    int my_id_, fd_ = -1;
    float field_w_, field_h_;
    bool running_ = true;

    rclcpp::Subscription<detect_result::msg::Locations>::SharedPtr sub_;
    std::thread thread_;
    std::mutex mtx_;
    detect_result::msg::Locations latest_;

    // 0x0305 兵种槽位(2026 协议 V2.0.0): 英雄/工程/3步/4步/空中/哨兵
    // 注意: 0x0305 无 5 号步兵位置, 空中为 6 号; 飞镖(8/108)/基地(10/110)/前哨(11/111)不发送
    static int slotOf(int id) {
        int n = id % 100;
        if (n == 1) return 0; if (n == 2) return 1;
        if (n == 3) return 2; if (n == 4) return 3;
        if (n == 6) return 4; if (n == 7) return 5;
        return -1;
    }

    void openSerial(const std::string& dev, int bps) {
        fd_ = open(dev.c_str(), O_RDWR | O_NOCTTY);
        if (fd_ < 0) {
            RCLCPP_ERROR(get_logger(), "Failed to open %s", dev.c_str());
            return;
        }
        struct termios t;
        tcgetattr(fd_, &t);
        cfsetospeed(&t, B115200);
        cfsetispeed(&t, B115200);
        t.c_cflag = CS8 | CLOCAL | CREAD;
        t.c_iflag = IGNPAR;
        t.c_oflag = 0;
        t.c_lflag = 0;
        t.c_cc[VTIME] = 1;
        t.c_cc[VMIN] = 0;
        tcflush(fd_, TCIFLUSH);
        tcsetattr(fd_, TCSANOW, &t);
        RCLCPP_INFO(get_logger(), "Serial %s opened @ %d", dev.c_str(), bps);
    }

    void cb(const detect_result::msg::Locations::SharedPtr msg) {
        std::lock_guard<std::mutex> l(mtx_);
        latest_ = *msg;
    }

    void loop() {
        while (rclcpp::ok() && running_) {
            detect_result::msg::Locations locs;
            {
                std::lock_guard<std::mutex> l(mtx_);
                locs = latest_;
            }

            // 12 槽位(48B): 0-5 对方[英雄/工程/3步/4步/空中/哨兵], 6-11 己方同序
            // 坐标单位 cm; 全 0 视为该车未发送(协议明示)
            uint16_t slots[12][2] = {};
            for (auto& l : locs.locs) {
                if (l.id < 0) continue;  // last_known 不发送
                bool enemy = (my_color_ == "Red") ? (l.id >= 100) : (l.id < 100 && l.id > 0);
                int s = slotOf(l.id);
                if (s < 0) continue;
                int idx = enemy ? s : 6 + s;
                int x = static_cast<int>(std::clamp(l.x, 0.0f, field_w_) * 100);
                int y = static_cast<int>(std::clamp(l.y, 0.0f, field_h_) * 100);
                slots[idx][0] = x; slots[idx][1] = y;
            }

            // 打包 0x0305 (48B)
            std::vector<uint8_t> data;
            for (int i = 0; i < 12; ++i) {
                data.push_back(slots[i][0] & 0xff);
                data.push_back((slots[i][0] >> 8) & 0xff);
                data.push_back(slots[i][1] & 0xff);
                data.push_back((slots[i][1] >> 8) & 0xff);
            }
            auto frame = buildFrame(0x0305, data);
            if (fd_ >= 0) write(fd_, frame.data(), frame.size());

            // 双倍易伤 (0x0301)
            std::vector<uint8_t> db_data;
            db_data.push_back(0x21); db_data.push_back(0x01);  // sub_cmd 0x0121
            db_data.push_back(my_id_ & 0xff);
            db_data.push_back((my_id_ >> 8) & 0xff);
            db_data.push_back(0x80); db_data.push_back(0x80);  // receiver 0x8080
            db_data.push_back(0x01); db_data.push_back(0x00);  // times=1
            auto db_frame = buildFrame(0x0301, db_data);
            if (fd_ >= 0) write(fd_, db_frame.data(), db_frame.size());

            std::this_thread::sleep_for(150ms);
        }
    }
};

int main(int argc, char** argv) {
    rclcpp::init(argc, argv);
    rclcpp::spin(std::make_shared<JudgeMessager>());
    rclcpp::shutdown();
    return 0;
}
